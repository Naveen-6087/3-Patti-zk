[package]
name = "zk_teen_patti"
type = "lib"
authors = ["Teen Patti ZK Team"]
compiler_version = ">=1.0.0"

[dependencies]
# No external dependencies for maximum compatibility with Noir v1.0.0-beta.6
# Using std::hash::pedersen_hash from stdlib (guaranteed available)
# Implementing our own Merkle tree (no external binary_merkle_root needed)
